{
  "name": "cohen-balancing-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Case management platform for Cohen Financial Balancing (monorepo root)",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=24.15.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "install:all": "./scripts/install",
    "dev": "./scripts/dev",
    "check": "./scripts/check",
    "test": "./scripts/test",
    "migrate": "./scripts/migrate",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
